{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run balanced fixture",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/out/driftdtl",
      "args": ["run", "tests/fixtures/balanced_settlement.json", "--json", "--events", "--pretty"],
      "cwd": "${workspaceFolder}",
      "stopAtEntry": false,
      "externalConsole": false
    }
  ]
}
